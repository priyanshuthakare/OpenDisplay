# Tests

System tests should cover unplug, reconnect, suspend, resume, mode switching, input mapping, and long-running latency stability.

Rust protocol unit tests live beside the protocol implementation.

